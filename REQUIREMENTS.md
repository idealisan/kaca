# 步骤记录器（Step Recorder）需求

一个客户端桌面小工具：记录用户的操作（按键 / 鼠标点击 / 鼠标滚动）并生成一份自包含的 HTML 文档。

## 1. 事件捕获
- 键盘：记录按下的任意键。
- 鼠标点击：左键 / 右键 / 中键（或其他键）。
- 鼠标滚动：
  - 判定规则：**1 秒时间窗口**内连续发生的滚动合并为「一次」滚动事件。
  - 停顿 **> 1 秒**后再滚动，算作新的一次。
  - 连续长时间滚动（中间无 >1s 停顿）仍算作一次。
  - 记录滚动方向（上/下/左/右）与数值：Mac/PC 上一次小滚动默认值约为 `100`，需记录该单位数值。

## 2. 截图
- 截取「当前窗口」或「桌面全屏」。
- 截图中必须包含：
  - 鼠标当前位置；
  - 鼠标指针图标（cursor image）；
  - 指针旁以小标记标明本次操作（左键 / 右键 / 滚轮 等）。

## 3. 收集的系统与上下文信息
- 操作系统信息（版本等）。
- 基本硬件配置：CPU、内存等。
- 被点击窗口的：标题、进程可执行文件名、pid 及其他进程相关信息。
- 被点击控件的文本（如按钮上的文字），能取到就取。

## 4. HTML 报告格式
- 所有截图 / 非文本信息一律以 **Base64 Data URL** 内嵌进 HTML（文档自包含、可离线打开）。
- 文档开头用一张「卡片」展示操作系统与硬件信息。
- 每一步展示：
  - 步骤序号（第 N 步）；
  - 用户系统本地时间；
  - 绝对时间戳；
  - 相对时间：以录制开始为 0:00，本步相对开始经历了多久；
  - 一句话操作描述（摁了某键 / 点了某个鼠标键 / 滚轮方向 + 单位数）；
  - 下方放置该步截图。
- 每行文本最右侧有一个「信息按钮」，点击弹出悬浮提示，显示本步附加信息（pid、窗口标题、控件文本等），**默认隐藏**。

## 5. 技术栈与 UI
- 语言：C（调用操作系统 API 的桌面应用）。
- UI：一条**横向条状工具条**（开始 / 停止 / 保存）。

## 6. 平台说明与架构约束（重要）
- **跨平台，优先级：macOS > Windows > Linux（Linux 优先级最低，桌面环境不统一）**。
- **必须做好操作系统 API 的隔离与抽象**：所有依赖 OS 的能力（事件捕获、截图、光标图像、窗口/控件信息、系统硬件信息、工具条 UI）都通过统一的抽象接口暴露，具体实现放到各平台子目录（`src/os/macos`、`src/os/windows`、`src/os/linux`）。公共逻辑（录制状态机、滚动分组、步骤列表、HTML 生成）不依赖任何平台 API。
- 当前先完整实现 **macOS**（开发机 macOS 27 arm64，Apple clang 21）；Windows/Linux 先给出遵循同一抽象接口的桩实现，后续补全。
- 纯 C 无法覆盖 AppKit 工具条 UI 与 `NSCursor` 光标图像，因此 macOS 实现中核心逻辑用 C，仅在被迫处用极小的 Objective-C 桥接文件（`.m`）。
- 事件捕获用 Core Graphics `CGEventTap`（C）；窗口信息用 `CGWindowListCopyWindowInfo`（C）；控件文本用 Accessibility `AXUIElement`（C）；硬件信息用 `sysctl`（C）。
